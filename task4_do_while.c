#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int findFirstElement(double eps)
{
int i = 0
do
{
    if (fabs(a(i)) <= eps)
        break;
    ++i;
} while (true)
return i;
}
